#include <stdio.h>
int main(){
    //creat a variable number that will store a number we get a output
    int mynum;
    // ask the user type a number
    printf("enter a number press the enter");
    //get and save the number you get the user
    scanf("%d", &mynum);
    //printf(the number is the user input)
    printf("your number is: %d", mynum);
    return 0;
    
    
}
